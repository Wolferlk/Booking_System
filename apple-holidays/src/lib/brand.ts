/**
 * Single source of truth for the application logo.
 *
 * All logo assets are generated from `Apple Logo .png` (transparent square
 * mark). Point new UI at these constants rather than hard-coding a path, so a
 * future rebrand is a one-file change.
 */

/** Primary square mark — sidebar, login screens, loaders, splash. */
export const APP_LOGO = '/png/apple-logo.png'

/** Same mark, small file — print headers and other low-resolution slots. */
export const APP_LOGO_SMALL = '/logo.png'

export const APP_NAME = 'Apple Holidays'
export const APP_TAGLINE = 'Apple System Operations'
